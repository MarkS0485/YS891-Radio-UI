<UserControl x:Class="YS891.RadioUI.Controls.WaterfallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource LcdGridBrush}" CornerRadius="3" ClipToBounds="True">
        <Grid>
            <Image x:Name="Bitmap" Stretch="Fill" RenderOptions.BitmapScalingMode="NearestNeighbor" />
            <TextBlock Text="AUDIO WATERFALL 0–5 kHz" FontFamily="Consolas" FontSize="11"
                       Foreground="{StaticResource LcdDimTextBrush}"
                       HorizontalAlignment="Left" VerticalAlignment="Top" Margin="6,3" />
        </Grid>
    </Border>
</UserControl>
